name: Probe GitHub Actions

on:
  push:
    branches:
      - ci/yita-watchdog-build

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner availability
        run: echo "GitHub Actions runner available"
